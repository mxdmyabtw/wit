# Site statique Win is Key - pas de build
[build]
  base = "static"
  publish = "."
  command = "echo 'Static site - no build'"

# Fonctions serverless pour Stripe
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
